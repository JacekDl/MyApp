@model MyApp.Web.ViewModels.InstructionsViewModel;

@{
    ViewData["Title"] = "Dawkowanie";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    var instructions = Model.Instructions;

    var totalPages = Model.TotalPages;
    var page = Model.Page;
    var pageSize = Model.PageSize;

    var window = 1;
    int Clamp(int v, int min, int max) => v < min ? min : (v > max ? max : v);

    var start = Clamp(page - window, 2, Math.Max(2, totalPages - 1));
    var end = Clamp(page + window, 2, Math.Max(2, totalPages - 1));

    if (start > end) { start = end; }
}

<h2 class="mb-3">Dawkowanie - dostępne kody</h2>

<form method="get" class="d-flex align-items-center gap-2 mb-3">
    <input type="hidden" name="page" value="1" />

    <label class="form-label mb-0">Na stronę:</label>
    <select name="pageSize" class="form-select form-select-sm" style="width:auto" onchange="this.form.submit()">
        <option value="5" selected="@(Model.PageSize == 5)">5</option>
        <option value="10" selected="@(Model.PageSize == 10)">10</option>
        <option value="20" selected="@(Model.PageSize == 20)">20</option>
        <option value="50" selected="@(Model.PageSize == 50)">50</option>
    </select>
</form>

<div class="container mt-3">
    <div class="mb-3">
        <form asp-action="AddInstruction" asp-controller="Medicine" method="post" class="row g-2">
            @Html.AntiForgeryToken()
            <div class="col-sm-3">
                <input name="Code" class="form-control" placeholder="Kod" maxlength="64" />
            </div>
            <div class="col-sm-7">
                <input name="Text" class="form-control" placeholder="Dawkowanie" maxlength="1024" />
            </div>
            <div class="col-sm-2 d-grid">
                <button type="submit" class="btn btn-secondary">Dodaj</button>
            </div>
        </form>
    </div>

    <div class="table-responsive">
        <table class="table align-middle mb-0">
            <thead>
                <tr>
                    <th scope="col" class="col-2">Kod</th>
                    <th scope="col" class="col-8">Dawkowanie</th>
                    <th scope="col" class="col-2" colspan="2"></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in instructions)
                {
                    <tr>
                        <td class="col-2">@item.Code</td>
                        <td class="col-8 text-break">@item.Text</td>
                        <td class="col-1 text-end">
                            <form asp-action="ModifyInstruction" asp-controller="Medicine" method="get" class="d-inline">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="id" value="@item.Id" />
                                <button type="submit" class="btn btn-sm btn-secondary">
                                    Zmień
                                </button>
                            </form>
                        </td>
                        <td class="col-1 text-start">
                            <button type="button"
                                    class="btn btn-sm btn-secondary"
                                    data-bs-toggle="modal"
                                    data-bs-target="#deleteInstructionModal-@item.Id">
                                Usuń
                            </button>

                            <div class="modal fade"
                                 id="deleteInstructionModal-@item.Id"
                                 tabindex="-1"
                                 aria-labelledby="deleteInstructionModalLabel-@item.Id"
                                 aria-hidden="true"
                                 data-bs-backdrop="static"
                                 data-bs-keyboard="false">

                                <div class="modal-dialog modal-dialog-centered">
                                    <div class="modal-content">

                                        <div class="modal-header">
                                            <h5 class="modal-title"
                                                id="deleteInstructionModalLabel-@item.Id">
                                                Potwierdzenie usunięcia
                                            </h5>
                                            <button type="button"
                                                    class="btn-close"
                                                    data-bs-dismiss="modal"
                                                    aria-label="Zamknij"></button>
                                        </div>

                                        <div class="modal-body">
                                            <p class="mb-2">
                                                Czy na pewno chcesz usunąć dawkowanie:
                                            </p>
                                            <p class="fw-bold text-break mb-0">
                                                @item.Code - @item.Text
                                            </p>
                                        </div>

                                        <div class="modal-footer">
                                            <button type="button"
                                                    class="btn btn-outline-secondary"
                                                    data-bs-dismiss="modal">
                                                Anuluj
                                            </button>

                                            <form asp-action="DeleteInstruction"
                                                  asp-controller="Medicine"
                                                  method="post"
                                                  class="m-0">
                                                @Html.AntiForgeryToken()
                                                <input type="hidden" name="id" value="@item.Id" />
                                                <button type="submit" class="btn btn-danger">
                                                    Usuń
                                                </button>
                                            </form>
                                        </div>

                                    </div>
                                </div>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

</div>

@if (totalPages > 1)
{
    <nav aria-label="Paginacja" class="mt-4">
        <ul class="pagination justify-content-center">

            <li class="page-item @(page <= 1 ? "disabled" : "")">
                <a class="page-link"
                   asp-action="Instructions"
                   asp-controller="Medicine"
                   asp-route-page="@(page - 1)"
                   asp-route-pageSize="@pageSize">«</a>
            </li>

            <li class="page-item @(page == 1 ? "active" : "")">
                <a class="page-link"
                   asp-action="Instructions"
                   asp-controller="Medicine"
                   asp-route-page="1"
                   asp-route-pageSize="@pageSize">1</a>
            </li>

            @if (start > 2)
            {
                <li class="page-item disabled"><span class="page-link">…</span></li>
            }

            @for (var p = Math.Max(start, 2); p <= Math.Min(end, totalPages - 1); p++)
            {
                <li class="page-item @(p == page ? "active" : "")">
                    <a class="page-link"
                       asp-action="Instructions"
                       asp-controller="Medicine"
                       asp-route-page="@p"
                       asp-route-pageSize="@pageSize">@p</a>
                </li>
            }

            @if (end < totalPages - 1)
            {
                <li class="page-item disabled"><span class="page-link">…</span></li>
            }

            <li class="page-item @(page == totalPages ? "active" : "")">
                <a class="page-link"
                   asp-action="Instructions"
                   asp-controller="Medicine"
                   asp-route-page="@totalPages"
                   asp-route-pageSize="@pageSize">@totalPages</a>
            </li>

            <li class="page-item @(page >= totalPages ? "disabled" : "")">
                <a class="page-link"
                   asp-action="Instructions"
                   asp-controller="Medicine"
                   asp-route-page="@(page + 1)"
                   asp-route-pageSize="@pageSize">»</a>
            </li>

        </ul>
    </nav>
}
