@{
    var current = (ViewContext.RouteData.Values["action"]?.ToString() ?? "").ToLowerInvariant();
}

<ul class="nav nav-pills mb-3">
    <li class="nav-item">
        <a class="nav-link @(current == "getreview" ? "active" : "")"
           asp-controller="Patient" asp-action="GetReview">Reviews</a>
    </li>
    <li class="nav-item">
        <a class="nav-link @(current == "tokens" ? "active" : "")"
           asp-controller="Patient" asp-action="Tokens">Tokens</a>
    </li>

</ul>