@{
    var current = (ViewContext.RouteData.Values["action"]?.ToString() ?? "").ToLowerInvariant();
}

<ul class="nav nav-pills mb-3">
    <li class="nav-item">
        <a class="nav-link @(current == "viewusers" ? "active" : "")" asp-controller="Admin" asp-action="ViewUsers">Users</a>
    </li>
    <li class="nav-item">
        <a class="nav-link @(current == "reviews" ? "active" : "")" asp-controller="Admin" asp-action="Reviews">Reviews</a>
    </li>
    <li class="nav-item">
        <a class="nav-link @(current == "medicines" ? "active" : "")" asp-controller="Admin" asp-action="Medicines">Medicines</a>
    </li>
    <li class="nav-item">
        <a class="nav-link @(current == "instructions" ? "active" : "")" asp-controller="Admin" asp-action="Instructions">Instructions</a>
    </li>
</ul>