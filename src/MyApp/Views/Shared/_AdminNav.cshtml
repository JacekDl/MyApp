@{
    var current = ViewContext.RouteData.Values["action"]?.ToString();
}

<ul class="nav nav-tabs mb-4">
    <li class="nav-item">
        <a class="nav-link @(current == "Users" ? "active" : "")" asp-controller="Admin" asp-action="Users">Użytkownicy</a>
    </li>
    <li class="nav-item">
        <a class="nav-link @(current == "Reviews" ? "active" : "")" asp-controller="Admin" asp-action="Reviews">Zalecenia</a>
    </li>
    <li class="nav-item">
        <a class="nav-link @(current == "Promotions" ? "active" : "")" asp-controller="Admin" asp-action="Promotions">Rejestracja farmaceutów</a>
    </li>
    <li class="nav-item dropdown">
        <a class="nav-link @((current == "Medicines") || (current == "Instructions") ? "active" : "")" dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-expanded="false">Słowniki</a>
        <ul class="dropdown-menu">
            <li><a class="dropdown-item" asp-controller="Medicine" asp-action="Medicines">Leki</a></li>
            <li><a class="dropdown-item" asp-controller="Medicine" asp-action="Instructions">Dawkowanie</a></li>
        </ul>
    </li>

</ul>